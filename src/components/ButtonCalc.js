import React from 'react';
import Button from 'react-bootstrap/Button';
import '../static/css/ButtonCalc.css';

const ButtonCalc = ({ calc, setCalc }) => {
  /* console.log(props);
  const handleButton = (e) => {
    switch (props.data) {
      case '0':
        //props.(props.data + e.target.value);
        break;

      default:
        break;
    }

    /* if (props.data === '0') {
      props.action(e.target.value);
    } else {
      props.action(props.data + e.target.value);
    }
    console.log(e.target.value); */

  return <Button>a</Button>;
};

export default ButtonCalc;
